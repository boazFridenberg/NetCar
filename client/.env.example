# NetCar — Client Environment

# Where the Vite dev server proxies /api requests. Must match the backend PORT.
VITE_API_PROXY_TARGET=http://localhost:3002

# Base path the app uses for API calls. Leave as /api to use the dev proxy.
VITE_API_BASE_URL=/api
